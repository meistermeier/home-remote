#JSGF V1.0;

grammar home;

public <home> = <maincontrol> | <videocontrol>;
<maincontrol> = remote control <maincommand>;
<maincommand> = (activate | sleep);
<videocontrol> = <video> ( play | pause | stop );
<video> = ( video player | movie );
